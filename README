This is a deblobbed ports tree intended for LibertyBSD 5.8 (although it will also
work with OpenBSD 5.8).

For deblobbing purposes, check the ./DEBLOB directory for more information.

---------------------------------------------------------------------------------

Documentation for the ports tree: ports(7), packages(7), mirroring-ports(7),
library-specs(7), bsd.port.mk(5), bsd.port.arch.mk(5), port-modules(5).

dpb(1) (manpage under ${PORTSDIR}/infrastructure/man) for bulk builds.